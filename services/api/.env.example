# 服务监听
PORT=3000
HOST=0.0.0.0

# Postgres（对应 docker-compose.yml 的默认值）
DATABASE_URL=postgres://bravecat:bravecat@localhost:5432/bravecat

# 经济占位校验：每现实小时可积累的小鱼干上限 + 新账号初始额度
ECONOMY_MAX_EARN_PER_HOUR=600
ECONOMY_INITIAL_EARN_ALLOWANCE=100
