# 画框布局原型 · 第三轮（实测两点透视）— 待确认

日期：2026-08-12/13。dev-only 原型，`?wallProto=a|b|c|d|e`。

## 2026-08-13 关键发现：底图是两点透视，文档柜面带不成立

用 `scripts/measure-wall-lines-prototype.mjs` 从底图
`public/dev-art/home-v4/interior-foreground.png` 像素实测
（逐列边缘检测 + 最小二乘拟合，标注图见
`measurement--wall-lines--annotated.png`）：

| 线 | 实测斜率 | 角度 |
| --- | --- | --- |
| 窗墙地脚线 | -0.2184 | -12.3°（朝墙角上升） |
| 右墙踢脚板上沿 | +0.3827 | +20.9° |
| 右墙踢脚板下沿 | +0.4205 | +22.8° |
| 柜面（边桌顶面）后沿 | +0.0318 | +1.8° |
| 柜面前沿 | +0.0970 | +5.5° |

右墙四条线两两求交全部聚在 **(≈230, 947)**；窗墙地脚线延长到同一高度
正好落在 x≈1682（以上为几何事实，可用脚本复跑）。

数值层面另一个事实：文档常量「柜面参考带 10.8°–13.9° 向右下」
（`HOME_CABINET_PERSPECTIVE_REFERENCE`，65/262 与 50/262）与实测柜面
+1.8°/+5.5° 不符。

> **⚠️ 解读存疑（2026-08-13 用户否决）**：基于上述拟合，本轮曾推断
> 「底图是自洽两点透视、墙角 x=768 是最深处、深度收缩应朝墙角（左）、
> v3 右列收小方向反了」，并据此做了 D（真透视楔形）与 E（解读图）。
> 用户明确判定**深度方向反了、该推理不成立**，并指出**底图自身的透视
> 方案也可以被质疑**（画出来的线不等于空间意图的 ground truth）。
> 空间解读待重新推导，D/E 两个变体反映的是被否决的解读，仅留作记录。

## 变体

- **A 窗绳挂卡**：棉绳横过窗前上沿，六张明信片木夹悬挂。卡片 100×75。
- **B 正面搁板**：右墙两层浅木搁板，明信片立放靠墙。卡片 110×82。
- **C 顶部展示带**：房间上方平面展示带，与房间透视解耦。卡片 170×122。
- **D 软木回忆板·真透视**：按实测灭点 (230,947) 把软木板真投影到右墙，
  上下边落在灭点束上、竖边垂直，越靠墙角越小（板右缘高 410 / 左缘 307）。
- **E 墙面透视解读·实测**：非布局变体。绿=实测线（含向灭点的延长），
  蓝=水平线/灭点/窗框顶证据，紫=真透视对上墙的要求（底图未采用），
  灰/红/橙=软木板三种处理对比（平贴 / 12° 斜投影(作废) / 真透视）。

截图为 470×900 运行时合成（正午光照，6 张明信片满状态）。选定后把几何
折回正式实现并重建 fixture 底图，原型代码（`src/lib/wallLayoutPrototype.ts`、
`src/lib/WallLayoutPrototype.svelte`、`scripts/capture-wall-layout-prototype.mjs`、
`scripts/measure-wall-lines-prototype.mjs`）随即删除。
